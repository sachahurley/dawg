# Decision log

Annotated decisions live as separate markdown files (situation, options, choice, why).

Start from **`TEMPLATE.md`**. Add new files with the next free number or a descriptive slug; `add_knowledge` assigns the next number itself for universal decisions. After a batch, **`reindex`** with `folder` set to `examples/decisions`.
