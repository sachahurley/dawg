# Decision log

Annotated decisions live as separate markdown files (situation, options, choice, why).

Start from **`TEMPLATE.md`**. Add new files with the next free number or a descriptive slug (currently **001–008** plus samples). After a batch, **`reindex`** with `filter_folder` `examples/decisions`.
