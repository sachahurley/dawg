# Corrections log

Short entries when an assistant’s answer was wrong, so future sessions avoid the same mistake.

Start from **`TEMPLATE.md`**. Name files with a date + slug. **`reindex`** with `folder` set to `examples/corrections` after adding entries.
