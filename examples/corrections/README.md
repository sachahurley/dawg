# Corrections log

Short entries when an assistant’s answer was wrong, so future sessions avoid the same mistake.

Start from **`TEMPLATE.md`**. Name files with a date + slug. **`reindex`** with `filter_folder` `examples/corrections` after adding entries.
