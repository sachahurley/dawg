# Example: MetaLab feedback (structured)

**Subject:** Dashboard exploration — feedback v1

Thanks for the exploration. Overall direction matches the **information hierarchy** we discussed. Below is feedback by priority.

**Must address before handoff**

1. **Token usage:** Several cards use hex values not in our semantic map (see attached list). Please remap to **semantic tokens** so engineering can implement without new foundation work.
2. **Density:** The 3-column grid breaks below 1200px; we need a defined **tablet** behavior (stack vs two-column).

**Should address**

3. **Chart colors:** Success/warning series should use **semantic status** colors, not ad-hoc greens.

**Nice to have**

4. Micro-animation on card hover — optional if we stay within **150–200ms** motion budget for this product.

Happy to walk through on a quick call if easier.
