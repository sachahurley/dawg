# Example: ask to engineering (spec + acceptance)

**Subject:** Button loading state — spec + small API ask

Hi — for the rewards flow we need the primary **Button** to expose a **loading** state that disables tap and shows the inline spinner per spec (link).  

**Ask:** Can we align with the existing DS `Button` variant table, or do we need a one-off? If one-off, I’d like to time-box and log debt.

**Acceptance:**

- Loading state matches spec frame `Button / Primary / Loading`.
- Double-tap doesn’t double-submit.
- Storybook example added under Components / Button.

**Spec:** [link]  
Thanks — happy to pair if easier.
