{
  "name": "sacha-agent-rag-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP server: chunk, embed (Ollama), store/search (ChromaDB)",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "ingest": "node dist/ingest-cli.js",
    "dev": "tsc && node dist/index.js",
    "chroma:install": "python3 -m venv .venv && . .venv/bin/activate && pip install -U pip && pip install -r requirements-chroma.txt",
    "chroma:start": "bash scripts/start-chroma.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "chromadb": "^3.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
